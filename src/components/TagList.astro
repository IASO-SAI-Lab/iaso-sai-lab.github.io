---
interface Props {
  items: string[];
}

const { items } = Astro.props;
---

{
  items.length > 0 && (
    <ul class="tag-list" aria-label="Topics">
      {items.map((item) => <li>{item}</li>)}
    </ul>
  )
}
